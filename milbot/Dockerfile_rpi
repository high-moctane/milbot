FROM balenalib/rpi-debian-python:3.7.2

RUN apt-get update && \
    apt-get install -y mecab mecab-ipadic redis-tools && \
    useradd -m admin
USER admin

WORKDIR /home/admin
COPY . .

RUN pip install --user -r requirements.txt

ENV HOST_ADDRESS host_address

CMD ["python3", "main.py"]